defaults:
  - env: thread_in_hole
  - _self_

env:
  max_episode_steps: 20
  render_mode: human
  add_obs_to_info_dict: False
  frame_skip: 2
  # start_in_phase: touch
  # end_in_phase: done

num_observations: 2_000
save_path: data/thread_in_hole/
