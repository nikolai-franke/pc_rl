defaults:
  - env: thread_in_hole
  - _self_

env:
  max_episode_steps: 3
  render_mode: human
  add_obs_to_info_dict: False
  frame_skip: 20

num_observations: 10_000
save_path: data/thread_in_hole/
