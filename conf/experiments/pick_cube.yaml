# @package _global_

defaults:
  - override /env: pick_cube
  - override /model/tokenizer: color
  - _self_

batch_B: 8
batch_T: 320

runner:
  n_steps: 3e6

optimizer:
  lr: 5e-5

eval:
  n_eval_envs: 1


tags: [pick_cube, color]
