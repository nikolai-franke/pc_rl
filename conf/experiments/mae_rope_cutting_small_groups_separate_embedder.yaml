# @package _global_

defaults:
  - override /dataset: rope_cutting
  - override /model/tokenizer: color_separate_embedder
  - _self_

model:
  tokenizer:
    group_size: 16
    sampling_ratio: 0.125

use_color: True

tags: [rope_cutting, color, small_groups, separate_color_embedder]
