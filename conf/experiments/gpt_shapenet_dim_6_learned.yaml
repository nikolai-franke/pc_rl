# @package _global_

defaults:
  - override /dataset: shapenet
  - override /model/tokenizer: point_gpt
  - override /model/pos_embedder: learned
  - _self_

model:
  tokenizer:
    point_dim: 6

tags: [shapenet, GPT, 6_dim, learned_PE]
