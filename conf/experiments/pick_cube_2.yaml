# @package _global_

defaults:
  - override /env: pick_cube_2
  - _self_

batch_B: 8
batch_T: 320

runner:
  n_steps: 5e6

optimizer:
  lr: 5e-5

algo:
  ent_coeff_lr: 1e-5
  ent_coeff: 0.04

eval:
  n_eval_envs: 1


tags: [pick_cube]
