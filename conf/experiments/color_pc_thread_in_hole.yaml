# @package _global_

defaults:
  - override /env: thread_in_hole
  - override /model/embedder: color
  - _self_

env:
  observation_type: color_point_cloud

runner:
  n_steps: 2e6

optimier:
  lr: 5e-5
