# @package _global_

defaults:
  - override /dataset: grasp_lift_and_touch
  - override /model/tokenizer: point_gpt_color
  - override /model/prediction_head: color
  - _self_

# hydra:
#   sweeper:
#     params:
#       learning_rate: 1e-3, 5e-4

tags: [grasp_lift_and_touch, color, GPT]
