# @package _global_

defaults:
  - override /env: thread_in_hole
  - _self_

runner:
  n_steps: 1e6

algo:
  ent_coeff: 0.4
  ent_coeff_lr: 2e-5
  clip_grad_norm: null

point_dim: 3

tags: [thread_in_hole, pointnet]
