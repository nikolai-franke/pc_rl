# @package _global_

defaults:
  - override /dataset: thread_in_hole_color
  - override /model/tokenizer: color
  - override /model/prediction_head: color
  - _self_

tags: [thread_in_hole, color, MAE]

