# @package _global_

defaults:
  - override /dataset: deflect_spheres
  - override /model/tokenizer: point_gpt_color
  - override /model/prediction_head: color
  - _self_

tags: [deflect_spheres, color, GPT]
