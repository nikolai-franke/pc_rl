# @package _global_

defaults:
  - override /dataset: push_chair
  - override /model/tokenizer: point_gpt
  - _self_

model:
  tokenizer:
    point_dim: 4

  prediction_head:
    point_dim: 6

hydra:
  sweeper:
    params:
      feature_loss_coeff: 0.5, 0.1, 0.05, 0.01, 0.005, 0.001, 0.0005, 0.0001

tags: [push_chair, GPT]
