# @package _global_

defaults:
  - override /env: open_cabinet
  - override /model/tokenizer: color
  - _self_

batch_B: 8
batch_T: 320

runner:
  n_steps: 2e6

eval:
  n_eval_envs: 1

hydra:
  sweeper:
    params:
      algo.ent_coeff_lr: 2e-5, 5e-5
      algo.ent_coeff: 0.1, 0.2

tags: [open_cabinet, color]
