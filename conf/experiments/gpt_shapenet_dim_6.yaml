# @package _global_

defaults:
  - override /dataset: shapenet
  - override /model/tokenizer: point_gpt_color
  - override /model/prediction_head: default
  - _self_

tags: [shapenet, color, GPT, 6_dim]
