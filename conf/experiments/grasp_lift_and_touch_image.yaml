# @package _global_

defaults:
  - override /env: grasp_lift_and_touch
  - _self_

env:
  observation_type: rgb_image

runner:
  n_steps: 2e6

algo:
  ent_coeff_lr: 1e-5
  ent_coeff: 0.1
  clip_grad_norm: null


tags: [grasp_lift_and_touch, image_obs]
