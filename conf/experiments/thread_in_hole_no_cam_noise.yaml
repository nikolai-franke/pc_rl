# @package _global_

defaults:
  - override /env: thread_in_hole
  - _self_

env:
  observation_type: point_cloud
  camera_reset_noise: null

runner:
  n_steps: 1e6

algo:
  ent_coeff: 0.4
  ent_coeff_lr: 2e-5

tags: [thread_in_hole]
