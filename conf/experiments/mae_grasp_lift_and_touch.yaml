# @package _global_

defaults:
  - override /dataset: grasp_lift_and_touch
  - override /model/tokenizer: color
  - override /model/prediction_head: color
  - _self_

tags: [graps_lift_and_touch, color, MAE]
