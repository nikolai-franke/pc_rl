# @package _global_

defaults:
  - override /env: thread_in_hole
  - override /model/tokenizer: default
  - _self_

env:
  observation_type: point_cloud

runner:
  n_steps: 2e6

optimizer:
  lr: 1e-4

algo:
  ent_coeff_lr: 2e-5

tags: [thread_in_hole]
